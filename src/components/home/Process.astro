---
const steps = [
    {
        icon: "search",
        title: "1. Auditoría & Estrategia",
        description:
            "Análisis profundo de los procesos actuales. Identificación de cuellos de botella y oportunidades de alto impacto.",
    },
    {
        icon: "build",
        title: "2. Ingeniería Sólida",
        description:
            "Desarrollo de la infraestructura base. APIs, bases de datos y seguridad antes de cualquier automatización.",
    },
    {
        icon: "rocket_launch",
        title: "3. Automatización IA",
        description:
            "Implementación de agentes inteligentes y workflows automatizados que operan 24/7 sobre el sistema base.",
    },
];
---

<section
    id="procesos"
    class="py-24 px-6 lg:px-20 max-w-7xl mx-auto w-full bg-white dark:bg-gray-900"
>
    <div class="text-center mb-20">
        <h2 class="text-3xl font-bold mb-4 text-gray-900 dark:text-white">
            Del Caos a la Claridad
        </h2>
        <p class="text-gray-600 dark:text-gray-400 font-medium">
            Mi metodología para transformar operaciones manuales en sistemas
            autónomos.
        </p>
    </div>

    <div class="relative">
        <!-- Connecting Line (Desktop) -->
        <div
            class="hidden md:block absolute top-1/2 left-0 w-full h-0.5 bg-gray-100 dark:bg-gray-800 -translate-y-1/2 z-0"
        >
        </div>

        <div class="grid grid-cols-1 md:grid-cols-3 gap-12 relative z-10">
            {
                steps.map((step) => (
                    <div class="bg-white dark:bg-gray-900 p-8 border border-gray-100 dark:border-gray-800 rounded-2xl flex flex-col items-center text-center gap-6 hover:border-cyan-200 dark:hover:border-cyan-900 shadow-sm hover:shadow-xl transition-all group">
                        <div class="size-20 rounded-full bg-white dark:bg-gray-900 border-4 border-gray-50 dark:border-gray-800 flex items-center justify-center text-cyan-700 dark:text-cyan-400 shadow-inner group-hover:scale-110 transition-transform">
                            <span class="material-symbols-outlined text-4xl">
                                {step.icon}
                            </span>
                        </div>
                        <div class="flex flex-col gap-3">
                            <h3 class="text-xl font-bold text-gray-900 dark:text-white tracking-tight">
                                {step.title}
                            </h3>
                            <p class="text-sm text-gray-500 dark:text-gray-400 leading-relaxed font-medium">
                                {step.description}
                            </p>
                        </div>
                    </div>
                ))
            }
        </div>
    </div>
</section>
