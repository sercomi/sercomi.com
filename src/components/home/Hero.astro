---
import Button from "../Button.astro";
---

<section
    class="relative min-h-[85vh] flex items-center justify-center w-full px-6 lg:px-20 border-b border-gray-100 dark:border-gray-800 overflow-hidden bg-white dark:bg-gray-900"
>
    <!-- Grid Background -->
    <div
        class="absolute inset-0 grid-bg pointer-events-none opacity-40 dark:opacity-20"
        style="mask-image: radial-gradient(circle at center, black 0%, transparent 80%); -webkit-mask-image: radial-gradient(circle at center, black 0%, transparent 80%);"
    >
    </div>

    <!-- Ambient Blobs -->
    <div
        class="absolute top-1/4 left-1/4 w-64 h-64 bg-cyan-100/50 dark:bg-cyan-900/20 rounded-full blur-[100px] pointer-events-none opacity-60"
    >
    </div>
    <div
        class="absolute bottom-1/4 right-1/4 w-64 h-64 bg-indigo-100/50 dark:bg-indigo-900/20 rounded-full blur-[100px] pointer-events-none opacity-60"
    >
    </div>

    <div
        class="relative max-w-4xl w-full text-center flex flex-col gap-8 items-center z-10"
    >
        <!-- Availability Badge -->
        <div
            class="inline-flex items-center gap-2 px-3 py-1 rounded-full border border-gray-200 dark:border-gray-700 bg-white/80 dark:bg-gray-800/80 backdrop-blur shadow-sm"
        >
            <span class="w-2 h-2 rounded-full bg-green-500 animate-pulse"
            ></span>
            <span
                class="text-[10px] font-bold text-gray-500 dark:text-gray-400 uppercase tracking-widest"
            >
                Disponible para proyectos B2B
            </span>
        </div>

        <!-- Main Heading -->
        <h1
            class="text-5xl md:text-7xl font-bold leading-[1.1] tracking-tight text-gray-900 dark:text-white"
        >
            Construyo software sólido.<br />
            <span class="text-cyan-700 dark:text-cyan-400"
                >Lo potencio con IA.</span
            >
        </h1>

        <!-- Subtitle -->
        <p
            class="text-lg md:text-xl text-gray-600 dark:text-gray-400 max-w-2xl font-medium leading-relaxed"
        >
            Soluciones escalables y automatización inteligente para optimizar tu
            negocio, desde la arquitectura hasta la implementación de LLMs.
        </p>

        <!-- CTAs -->
        <div class="flex flex-col sm:flex-row gap-4 pt-4">
            <Button href="#capacidades" class="group h-12 px-8">
                Ver cómo puedo ayudarte
                <span
                    class="material-symbols-outlined ml-2 group-hover:translate-x-1 transition-transform"
                >
                    arrow_forward
                </span>
            </Button>
            <Button href="#casos-de-uso" variant="secondary" class="h-12 px-8">
                Ver Portfolio
            </Button>
        </div>

        <!-- Tech Stack / Specifics -->
        <div
            class="pt-12 flex flex-wrap justify-center gap-8 opacity-70 grayscale hover:grayscale-0 transition-all duration-500"
        >
            <div
                class="flex items-center gap-2 text-sm text-gray-500 dark:text-gray-400 font-bold tracking-tight"
            >
                <span class="material-symbols-outlined text-lg">code</span>
                Python / Go
            </div>
            <div
                class="flex items-center gap-2 text-sm text-gray-500 dark:text-gray-400 font-bold tracking-tight"
            >
                <span class="material-symbols-outlined text-lg">cloud</span>
                AWS / Azure
            </div>
            <div
                class="flex items-center gap-2 text-sm text-gray-500 dark:text-gray-400 font-bold tracking-tight"
            >
                <span class="material-symbols-outlined text-lg">psychology</span
                >
                OpenAI / LangChain
            </div>
            <div
                class="flex items-center gap-2 text-sm text-gray-500 dark:text-gray-400 font-bold tracking-tight"
            >
                <span class="material-symbols-outlined text-lg">schema</span>
                Docker / K8s
            </div>
        </div>
    </div>
</section>

<style>
    .grid-bg {
        background-size: 40px 40px;
        background-image:
            linear-gradient(
                to right,
                rgba(229, 231, 235, 0.8) 1px,
                transparent 1px
            ),
            linear-gradient(
                to bottom,
                rgba(229, 231, 235, 0.8) 1px,
                transparent 1px
            );
    }

    :global(.dark) .grid-bg {
        background-image:
            linear-gradient(
                to right,
                rgba(55, 65, 81, 0.3) 1px,
                transparent 1px
            ),
            linear-gradient(
                to bottom,
                rgba(55, 65, 81, 0.3) 1px,
                transparent 1px
            );
    }
</style>
