---
const stages = [
  {
    title: "La Base (10+ años)",
    description: "Una década de ingeniería de software pura. Construcción de sistemas complejos, escalables y mantenibles. La base sólida sobre la que se construye cualquier innovación real.",
    active: false,
  },
  {
    title: "La Evolución",
    description: "La transición natural hacia la IA y la automatización como herramientas para potenciar la capacidad humana y la eficiencia del negocio, más allá del hype tecnológico.",
    active: false,
  },
  {
    title: "La Visión Actual",
    description: "Filosofía 'Engineering first, AI second'. Construir sistemas inteligentes sobre cimientos de ingeniería sólidos. Pragmatismo, Seniority y Orientación a Negocio.",
    active: true,
  },
];
---

<div class="relative py-4 pl-2">
  <!-- Vertical Line -->
  <div class="absolute left-[11px] top-6 bottom-6 w-[2px] bg-border-subtle dark:bg-border-subtle-dark"></div>
  <div class="flex flex-col gap-10">
    {stages.map((stage) => (
      <div class="relative pl-10 group">
        <!-- Circle Indicator -->
        <div class={`absolute left-0 top-1.5 h-6 w-6 rounded-full border-[3px] shadow-sm z-10 transition-colors duration-300 flex items-center justify-center
          ${stage.active 
            ? "bg-brand-primary dark:bg-cyan-500 border-cyan-100 dark:border-gray-900 ring-4 ring-brand-primary/20" 
            : "border-white dark:border-gray-900 bg-gray-300 dark:bg-gray-700 group-hover:bg-brand-primary dark:group-hover:bg-cyan-500"
          }`}>
          {stage.active && <span class="material-symbols-outlined text-white text-[14px] font-bold">check</span>}
        </div>

        <div class={`flex flex-col gap-2 transition-all duration-300 ${stage.active ? "p-4 -ml-4 rounded-xl bg-surface-soft/80 dark:bg-surface-soft-dark border border-border-subtle dark:border-border-subtle-dark/50" : ""}`}>
          <h3 class={`text-lg font-bold leading-tight font-display ${stage.active ? "text-brand-primary dark:text-cyan-400" : "text-gray-900 dark:text-white"}`}>
            {stage.title}
          </h3>
          <p class={`text-sm md:text-base leading-relaxed font-body font-normal ${stage.active ? "text-gray-900 dark:text-gray-200" : "text-text-muted dark:text-text-muted-dark"}`}>
            {stage.description}
          </p>
        </div>
      </div>
    ))}
  </div>
</div>
